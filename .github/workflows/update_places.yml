name: judo weekly curation

on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:
    inputs:
      category:
        description: "특정 카테고리만 실행"
        required: false
        type: string

jobs:
  curate:
    runs-on: ubuntu-latest

    env:
      NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
      NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      INPUT_CATEGORY: ${{ github.event.inputs.category }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show files
        run: ls -R

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests openai supabase

      - name: Sanity check env
        run: |
          python - <<'PY'
          import os
          keys = [
              "NAVER_CLIENT_ID",
              "NAVER_CLIENT_SECRET",
              "SUPABASE_URL",
              "SUPABASE_KEY",
              "OPENAI_API_KEY",
              "INPUT_CATEGORY",
          ]
          for k in keys:
              print(f"{k}: {os.getenv(k)}" if k == "INPUT_CATEGORY" else f"{k}: {'OK' if os.getenv(k) else 'MISSING'}")
          PY

      - name: Run curation script
        run: python agent_test.py